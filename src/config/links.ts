/** The repository is public; the app points at it rather than asking for trust. */
export const REPO_URL = 'https://github.com/de-don/uniclaim'
export const ISSUES_URL = `${REPO_URL}/issues`
export const SECURITY_URL = `${REPO_URL}/security/advisories/new`
