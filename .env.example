# WalletConnect project id — free at https://cloud.reown.com
# Without it only browser-injected wallets (MetaMask, Rabby…) will connect.
VITE_WC_PROJECT_ID=

# Optional per-chain RPC override, takes priority over the bundled public nodes.
# VITE_RPC_1=https://eth-mainnet.g.alchemy.com/v2/<key>
# VITE_RPC_42161=
# VITE_RPC_137=
